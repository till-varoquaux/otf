# .readthedocs.yml
version: 2

build:
  os: ubuntu-20.04
  tools:
    python: "3.10"
  apt_packages:
    - graphviz


python:
  install:
    - method: pip
      path: .
    - requirements: docs/requirements.txt
